{
  "name": "backend",
  "version": "1.0.0",
  "description": "GlobeTrotter Backend API with Express, Prisma ORM and SQLite",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "setup": "npm install && npm run prisma:generate && npm run prisma:push && npm run prisma:seed",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:seed": "node prisma/seed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["travel", "express", "prisma", "sqlite"],
  "author": "GlobeTrotter Team",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "@prisma/adapter-better-sqlite3": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "prisma": "^7.9.1"
  }
}
